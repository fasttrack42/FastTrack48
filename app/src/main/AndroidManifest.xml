<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

	<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
	<uses-permission android:name="android.permission.VIBRATE"/>

	<!-- Injected by androidx.work for its NetworkType constraints, which this app never uses. -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove"/>

	<application
			android:name=".FastTrackApp"
			android:allowBackup="true"
			android:backupAgent=".FastTrackBackupAgent"
			android:fullBackupContent="@xml/backup_rules"
			android:dataExtractionRules="@xml/data_extraction_rules"
			android:icon="@mipmap/ic_launcher"
			android:label="@string/app_name"
			android:roundIcon="@mipmap/ic_launcher_round"
			android:supportsRtl="true"
			android:localeConfig="@xml/locales_config"
			android:theme="@style/Theme.FastTrackTheme"
			android:enableOnBackInvokedCallback="true">

		<!--
			Per-app language storage for API < 33, where there is no LocaleManager to hold it.
			AppCompat persists the selection itself and re-applies it at startup; above API 33 the
			framework owns it and this declaration is inert. Disabled by design — it is a marker
			AppCompat looks up, never a service anything starts.
		-->
		<service
				android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
				android:enabled="false"
				android:exported="false">
			<meta-data
					android:name="autoStoreLocales"
					android:value="true"/>
		</service>

		<receiver
				android:name=".widget.FastingWidgetReceiver"
				android:exported="true">
			<intent-filter>
				<action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
			</intent-filter>
			<meta-data
					android:name="android.appwidget.provider"
					android:resource="@xml/fasting_widget_info"/>
		</receiver>

		<activity
				android:name=".screens.intro.IntroActivity"
				android:label="@string/title_activity_intro"/>
		<activity
				android:name=".screens.info.InfoActivity"
				android:label="@string/title_activity_info"/>
		<activity
			android:name=".screens.settings.SettingsActivity"
			android:label="@string/title_activity_settings" />

		<service
				android:name=".AlertService"
				android:enabled="true"
				android:exported="false"
				android:permission="android.permission.BIND_JOB_SERVICE"/>

		<activity
				android:name=".screens.main.MainActivity"
				android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
			<meta-data
					android:name="android.app.shortcuts"
					android:resource="@xml/shortcuts"/>
		</activity>
		<!--
			Tapping a logbook file in a file manager, or sharing one to this app. Declared MIME
			types only decide whether the chooser lists us; the actual format is detected from the
			file's bytes, so a manager that mislabels an export still imports correctly. text/plain
			is deliberately absent — matching it would offer this app for every .txt on the device.
		-->
		<activity
				android:name=".screens.settings.ImportActivity"
				android:exported="true"
				android:excludeFromRecents="true"
				android:taskAffinity=""
				android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode|locale|layoutDirection|fontScale"
				android:theme="@style/Theme.FastTrackTheme.Transparent">
			<intent-filter android:label="@string/action_import">
				<action android:name="android.intent.action.VIEW"/>

				<category android:name="android.intent.category.DEFAULT"/>
				<category android:name="android.intent.category.BROWSABLE"/>

				<data android:scheme="content"/>
				<data android:scheme="file"/>
				<data android:mimeType="text/csv"/>
				<data android:mimeType="text/comma-separated-values"/>
				<data android:mimeType="application/csv"/>
				<data android:mimeType="application/json"/>
				<data android:mimeType="text/json"/>
				<data android:mimeType="application/zip"/>
				<data android:mimeType="application/x-zip-compressed"/>
				<data android:mimeType="multipart/x-zip"/>
				<data android:mimeType="text/calendar"/>
			</intent-filter>
			<intent-filter android:label="@string/action_import">
				<action android:name="android.intent.action.SEND"/>

				<category android:name="android.intent.category.DEFAULT"/>

				<data android:mimeType="text/csv"/>
				<data android:mimeType="text/comma-separated-values"/>
				<data android:mimeType="application/csv"/>
				<data android:mimeType="application/json"/>
				<data android:mimeType="text/json"/>
				<data android:mimeType="application/zip"/>
				<data android:mimeType="application/x-zip-compressed"/>
				<data android:mimeType="multipart/x-zip"/>
				<data android:mimeType="text/calendar"/>
			</intent-filter>
		</activity>
		<provider
				android:name="androidx.core.content.FileProvider"
				android:authorities="${applicationId}.fileprovider"
				android:exported="false"
				android:grantUriPermissions="true">
			<meta-data
					android:name="android.support.FILE_PROVIDER_PATHS"
					android:resource="@xml/file_paths"/>
		</provider>
		<provider
				android:name="androidx.startup.InitializationProvider"
				android:authorities="${applicationId}.androidx-startup"
				android:exported="false"
				tools:node="merge">
			<meta-data
					android:name="androidx.work.WorkManagerInitializer"
					android:value="androidx.startup"
					tools:node="remove"/>
		</provider>
	</application>

</manifest>
